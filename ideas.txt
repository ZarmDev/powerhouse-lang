
// artifically set input
setIn("0 10\n20 0")

// read a file and set to input
setIn(file("test.txt").read())

x: str = "test"
y: int = 5
logn y * 5
logn (0.5555 * 0.5)
logn "test" + "  2"

// classes
class car {
	private type = ""

	// constructor
	$car() {
		
	}

	// deconstructor
	~car() {
		this.test = test;
	}

	public printInfo(String info) {
		logn(info)
	}
}

test.printInfo()

// scope and const
const x: int = 5

// If the function is isolated, it CANNOT use outside variables - it helps performance.
isolated fn yolo() {
	// error: variable does not exist.
	x = 5
}

// you CANNOT free variables or functions. However, you 

struct Vec2 {
	
}

// Pointers:
